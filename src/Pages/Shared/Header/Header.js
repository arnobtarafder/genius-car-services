import React from 'react';

const Header = () => {
    return (
        <header>
            <h2>It's me the boy header of not header means null with undefined fact</h2>
        </header>
    );
};

export default Header;